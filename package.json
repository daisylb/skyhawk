{
  "name": "skyhawk",
  "version": "0.1.0",
  "description": "A library for managing network request in-flight state and errors with redux-thunk",
  "main": "index.js",
  "types": "index.d.ts",
  "scripts": {
    "prepare": "tsc && cp build/* .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "peerDependencies": {
    "immutable": "^3.8.1"
  },
  "author": "Adam Brenecki",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^2.5.2"
  }
}
